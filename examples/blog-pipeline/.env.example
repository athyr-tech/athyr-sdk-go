# Blog Pipeline Configuration

# Athyr server address (host.docker.internal reaches local Mac from Docker)
ATHYR_ADDR=host.docker.internal:9090

# LLM model (smaller = faster)
MODEL=qwen3:0.6b

# Output path for generated blog post (Docker uses /app/output/ for volume mount)
# OUTPUT_PATH=/app/output/blog-post.md